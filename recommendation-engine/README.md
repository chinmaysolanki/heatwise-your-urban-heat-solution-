# Recommendation engine (layout / templates)

**Canonical** TypeScript module for spatial layout, templates, constraints, and feedback — imported as `@/recommendation-engine`.

Ranked **ML** recommendations use `ml/serving` via `generateRecommendationsRuntime`; see [`docs/RECOMMENDATION_STACKS.md`](../docs/RECOMMENDATION_STACKS.md) and [`docs/ARCHITECTURE_RECOMMENDATION_ENGINES.md`](../docs/ARCHITECTURE_RECOMMENDATION_ENGINES.md).
